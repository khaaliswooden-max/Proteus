# Phase 5 F0 baseline harness — pinned dependencies.
# All pins use `==`. Update only with a documented engineering-choice DELTA.
# See harness/ENVIRONMENT.md for provenance.

llama-cpp-python==0.3.2
numpy==1.26.4
pydantic==2.9.2
scipy==1.13.1
